# ThreeC — research_workers 换行符保护
#
# 背景：本仓库 `core.autocrlf = true`。
# 根 `.gitattributes` 已声明 `research/**  -text`，
# 但该模式**不匹配** `research_workers/`（前缀不同，不是子目录）。
#
# 若不在此显式声明，全新克隆 / 任何 git 重新 checkout 会把 LF 改写成 CRLF，
# 导致每个 Workspace 的：
#   - `workspace_checksums.sha256`（规则版本一致性）
#   - `tools/fixtures/minimal_valid_package/checksums.sha256`
# 逐字节校验**必然失败**，直接破坏
# 「Workspace 搬到新环境后，Research Agent 使用的是与 R00 相同的规则版本」这一保证。
#
# 因此：整个 research_workers/ 一律不做 EOL 转换（-text）。

*  -text
